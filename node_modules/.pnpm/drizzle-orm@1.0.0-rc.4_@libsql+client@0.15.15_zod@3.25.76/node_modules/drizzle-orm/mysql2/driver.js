import { mysql2Codecs } from "./codecs.js";
import { MySql2Session } from "./session.js";
import { entityKind } from "../entity.js";
import { jitCompatCheck } from "../utils.js";
import { DefaultLogger } from "../logger.js";
import { MySqlAsyncDatabase, MySqlAsyncDatabase as MySqlDatabase } from "../mysql-core/async/db.js";
import { MySqlDialect } from "../mysql-core/dialect.js";
import { createPool } from "mysql2/promise";

//#region src/mysql2/driver.ts
var MySql2Database = class extends MySqlAsyncDatabase {
	static [entityKind] = "MySql2Database";
};
function construct(client, config = {}) {
	client.config.supportBigNumbers = true;
	const dialect = new MySqlDialect({
		useJitMappers: jitCompatCheck(config.jit),
		codecs: mysql2Codecs
	});
	let logger;
	if (config.logger === true) logger = new DefaultLogger();
	else if (config.logger !== false) logger = config.logger;
	const clientForInstance = isCallbackClient(client) ? client.promise() : client;
	const relations = config.relations ?? {};
	const db = new MySql2Database(dialect, new MySql2Session(clientForInstance, dialect, relations, {
		logger,
		cache: config.cache
	}), relations);
	db.$client = client;
	db.$cache = config.cache;
	if (db.$cache) db.$cache["invalidate"] = config.cache?.onMutate;
	return db;
}
function isCallbackClient(client) {
	return typeof client.promise === "function";
}
function drizzle(...params) {
	if (typeof params[0] === "string") {
		const connectionString = params[0];
		return construct(createPool({ uri: connectionString }), params[1]);
	}
	const { connection, client, ...DrizzleMySqlConfig } = params[0];
	if (client) return construct(client, DrizzleMySqlConfig);
	return construct(typeof connection === "string" ? createPool({ uri: connection }) : createPool(connection), DrizzleMySqlConfig);
}
(function(_drizzle) {
	function mock(config) {
		return construct({ config: {} }, config);
	}
	_drizzle.mock = mock;
})(drizzle || (drizzle = {}));

//#endregion
export { MySql2Database, MySqlDatabase, drizzle };
//# sourceMappingURL=driver.js.map
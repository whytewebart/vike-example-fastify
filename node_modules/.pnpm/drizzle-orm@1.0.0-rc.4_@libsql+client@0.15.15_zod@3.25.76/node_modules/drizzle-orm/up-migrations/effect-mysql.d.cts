import { UpgradeResult } from "./utils.cjs";
import { MigrationMeta } from "../migrator.cjs";
import { Effect } from "effect";
import { QueryEffectHKTBase } from "../effect-core/query-effect.cjs";
import { MySqlEffectSession } from "../mysql-core/effect/session.cjs";

//#region src/up-migrations/effect-mysql.d.ts
/**
 * Detects the current version of the migrations table schema and upgrades it if needed.
 *
 * Version 0: Original schema (id, hash, created_at)
 * Version 1: Extended schema (id, hash, created_at, name, applied_at)
 */
declare const upgradeIfNeeded: <TEffectHKT extends QueryEffectHKTBase>(migrationsTable: string, session: MySqlEffectSession<TEffectHKT>, localMigrations: MigrationMeta[]) => Effect.Effect<UpgradeResult, TEffectHKT['error'], TEffectHKT['context']>;
//#endregion
export { upgradeIfNeeded };
//# sourceMappingURL=effect-mysql.d.cts.map